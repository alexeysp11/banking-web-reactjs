import React, { Component } from "react";

class Contacts extends Component {
    render() {
        return (
            <React.Fragment>
                <h4>Contacts</h4>
            </React.Fragment>
        );
    }
};

export default Contacts; 
